// Configure API base for auth.html; override via ?api= on URL if needed.
// Default to same-origin; can be overridden by setting window.BIOARK_API_BASE before this file
window.BIOARK_API_BASE = window.BIOARK_API_BASE || '';
